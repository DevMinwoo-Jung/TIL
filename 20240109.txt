- 배열은 순서를 보장 못하고 맵은 순서를 보장한다. 그리나 맵은 sort하는 기능이 없다
- false인 것들은 뭐가 있을까?
    
    false, 0, null, “”, “, NaN
    
- true는??
    
    1, true, {}, []
    
- array의 return 값이 하나만 쓴다면 find를써
- 배열 체이닝의 단점
    
    반한된 배열 전체를 반복한다
    
- 함수 내에서 파라미터에 따라 코드가 가변적 이라면??
    
    1) 파라미터를 객체로 넘기던가
    
    2) 기본값을 써라
    
    ex) 화씨, 섭씨 변환
    
    1) param = {
    
    is화씨 : true,
    
    온도: 36.7
    
    }
    
    2) function convert화씨섭씨(param1 = true, temp){
    
    }
    
    근데 개인적으로는 그냥 객체로 넘겨받는게 편할
    
- 객체를 이름을 바꾸며 구조분해 할당 하는 법
    
    const { location: [latitude, longitude] ]} = landscape
    
- 고차함수?? → 어렵다 찾아봐서 정리할 것
    
    다른 함수를 반환하는 함,
    
    매개변수를 가두는 방법을 통해 특별한 값을 제공해, 나중에 원래 인수에 접근할 수 있게 해주고 실행을 마칠 수 있다.
    
    어렵다…
    
- async await는 Promise를 완벽하게 대체해주지 않는다
    
    1) 조금 더 쉬운 사용법을 사용해 부분적으로 Promise를 사용한다고 보면 됨
    
    2) 브라우저 지원이 충분하지 않아 컴파일 할 경우 에러 발생할 수 있음 
    
- fetch의 응답코드